spring:
  application:
    name: qlphonghoc

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/QLPHONGHOC?useSSL=false&serverTimezone=Asia/Bangkok&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:daniel}
    password: ${DB_PASSWORD:Thai06112005}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    show-sql: false

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: ${SERVER_PORT:8080}

app:
  frontend-base-url: ${FRONTEND_BASE_URL:http://localhost:5173}

  mail:
    enabled: ${MAIL_ENABLED:false}
    from: ${MAIL_FROM:no-reply@ucas.local}

  jwt:
    secret: ${JWT_SECRET:YjY0MTE3YmY1MGE4Zjg3NDQyZDJkM2VkNjM5ZDJmMTgzZjczYWE0Zjk0YmNlODZiNDE0YzE5Mzk4YTVjZTQ5ZA==}
    expiration: ${JWT_EXPIRATION:86400000}

  seed:
    enabled: ${SEED_ENABLED:false}